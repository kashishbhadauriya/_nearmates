<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>About NearMates - Connecting Communities Through Shared Interests</title>
  
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
  
  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <style>
/* ===== MODERN RESET ===== */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

/* ===== ROOT VARIABLES ===== */
:root {
  --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --secondary-gradient: linear-gradient(135deg, #4ecdc4 0%, #44a08d 100%);
  --accent-gradient: linear-gradient(135deg, #ff6b6b 0%, #ee5a24 100%);
  --dark-bg: #0a0a0a;
  --card-bg: rgba(255, 255, 255, 0.05);
  --text-primary: #ffffff;
  --text-secondary: #b0b0b0;
  --text-muted: #8a8a8a;
  --border-color: rgba(255, 255, 255, 0.1);
  --hover-bg: rgba(255, 255, 255, 0.08);
}

/* ===== BODY & LAYOUT ===== */
body {
  font-family: 'Inter', sans-serif;
  background: var(--dark-bg);
  background-image: 
    radial-gradient(circle at 20% 20%, rgba(102, 126, 234, 0.1), transparent 50%),
    radial-gradient(circle at 80% 80%, rgba(78, 205, 196, 0.1), transparent 50%),
    radial-gradient(circle at 40% 60%, rgba(255, 107, 107, 0.05), transparent 50%);
  background-attachment: fixed;
  color: var(--text-primary);
  line-height: 1.6;
  overflow-x: hidden;
}

/* ===== CONTAINER ===== */
.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 2rem;
}

/* ===== HEADER SECTION ===== */
.header {
  text-align: center;
  padding: 4rem 0 2rem;
  position: relative;
}

.header::before {
  content: '';
  position: absolute;
  top: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 100px;
  height: 4px;
  background: var(--primary-gradient);
  border-radius: 2px;
}

.main-title {
  font-family: 'Playfair Display', serif;
  font-size: clamp(2.5rem, 5vw, 4rem);
  font-weight: 700;
  background: var(--primary-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: 1rem;
  animation: fadeInUp 0.8s ease-out;
}

.subtitle {
  font-size: 1.25rem;
  color: var(--text-secondary);
  max-width: 600px;
  margin: 0 auto 3rem;
  animation: fadeInUp 0.8s ease-out 0.2s both;
}

/* ===== SECTION STYLES ===== */
.section {
  margin-bottom: 4rem;
  opacity: 0;
  transform: translateY(30px);
  animation: slideInUp 0.8s forwards;
}

.section:nth-child(even) {
  animation-delay: 0.1s;
}

.section:nth-child(odd) {
  animation-delay: 0.2s;
}

.section-title {
  font-family: 'Playfair Display', serif;
  font-size: 2.2rem;
  font-weight: 600;
  margin-bottom: 1.5rem;
  position: relative;
  display: flex;
  align-items: center;
  gap: 1rem;
}

.section-title::before {
  content: '';
  width: 6px;
  height: 40px;
  background: var(--secondary-gradient);
  border-radius: 3px;
  flex-shrink: 0;
}

.section-icon {
  font-size: 1.5rem;
  background: var(--accent-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

/* ===== CARD STYLES ===== */
.content-card {
  background: var(--card-bg);
  backdrop-filter: blur(20px);
  border: 1px solid var(--border-color);
  border-radius: 20px;
  padding: 2rem;
  margin-bottom: 2rem;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.content-card::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  height: 2px;
  background: var(--primary-gradient);
  opacity: 0;
  transition: opacity 0.3s ease;
}

.content-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
  border-color: rgba(255, 255, 255, 0.2);
}

.content-card:hover::before {
  opacity: 1;
}

.content-card p {
  color: var(--text-secondary);
  font-size: 1.1rem;
  line-height: 1.7;
}

/* ===== LIST STYLES ===== */
.feature-list {
  list-style: none;
  display: grid;
  gap: 1rem;
  margin-top: 1.5rem;
}

.feature-item {
  background: var(--card-bg);
  backdrop-filter: blur(10px);
  border: 1px solid var(--border-color);
  border-radius: 15px;
  padding: 1.5rem;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.feature-item::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 4px;
  background: var(--secondary-gradient);
  opacity: 0;
  transition: opacity 0.3s ease;
}

.feature-item:hover {
  transform: translateX(10px);
  background: var(--hover-bg);
  border-color: rgba(78, 205, 196, 0.3);
}

.feature-item:hover::before {
  opacity: 1;
}

.feature-icon {
  display: inline-block;
  width: 40px;
  height: 40px;
  background: var(--secondary-gradient);
  border-radius: 50%;
  color: white;
  text-align: center;
  line-height: 40px;
  margin-right: 1rem;
  font-size: 1rem;
  vertical-align: top;
}

.feature-content {
  display: inline-block;
  width: calc(100% - 60px);
  vertical-align: top;
}

.feature-title {
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 0.5rem;
}

.feature-description {
  color: var(--text-secondary);
  font-size: 0.95rem;
}

/* ===== USE CASE CARDS ===== */
.use-case-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 1.5rem;
  margin-top: 2rem;
}

.use-case-card {
  background: var(--card-bg);
  backdrop-filter: blur(15px);
  border: 1px solid var(--border-color);
  border-radius: 20px;
  padding: 2rem;
  transition: all 0.3s ease;
  position: relative;
}

.use-case-card::after {
  content: '';
  position: absolute;
  inset: -1px;
  background: var(--primary-gradient);
  border-radius: 20px;
  padding: 1px;
  mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
  mask-composite: exclude;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.use-case-card:hover {
  transform: translateY(-8px) scale(1.02);
}

.use-case-card:hover::after {
  opacity: 1;
}

.use-case-icon {
  width: 60px;
  height: 60px;
  background: var(--accent-gradient);
  border-radius: 15px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-size: 1.5rem;
  margin-bottom: 1rem;
}

.use-case-title {
  font-size: 1.3rem;
  font-weight: 600;
  color: var(--text-primary);
  margin-bottom: 0.8rem;
}

.use-case-description {
  color: var(--text-secondary);
  line-height: 1.6;
}

/* ===== STATS SECTION ===== */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 2rem;
  margin: 3rem 0;
}

.stat-card {
  text-align: center;
  padding: 2rem;
  background: var(--card-bg);
  border-radius: 20px;
  border: 1px solid var(--border-color);
  transition: all 0.3s ease;
}

.stat-card:hover {
  transform: translateY(-5px);
  box-shadow: 0 15px 30px rgba(0, 0, 0, 0.2);
}

.stat-number {
  font-size: 2.5rem;
  font-weight: 800;
  background: var(--primary-gradient);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  display: block;
  margin-bottom: 0.5rem;
}

.stat-label {
  color: var(--text-secondary);
  font-weight: 500;
}

/* ===== BUTTON STYLES ===== */
.cta-button {
  display: inline-flex;
  align-items: center;
  gap: 0.8rem;
  padding: 1rem 2rem;
  background: var(--primary-gradient);
  color: white;
  text-decoration: none;
  border-radius: 50px;
  font-weight: 600;
  font-size: 1.1rem;
  transition: all 0.3s ease;
  position: relative;
  overflow: hidden;
}

.cta-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
  transition: left 0.6s;
}

.cta-button:hover {
  transform: translateY(-3px) scale(1.05);
  box-shadow: 0 15px 35px rgba(102, 126, 234, 0.4);
  color: white;
  text-decoration: none;
}

.cta-button:hover::before {
  left: 100%;
}

/* ===== ANIMATIONS ===== */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideInUp {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* ===== RESPONSIVE DESIGN ===== */
@media (max-width: 768px) {
  .container {
    padding: 1rem;
  }
  
  .section-title {
    font-size: 1.8rem;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.5rem;
  }
  
  .content-card {
    padding: 1.5rem;
  }
  
  .feature-item {
    padding: 1rem;
  }
  
  .use-case-grid {
    grid-template-columns: 1fr;
  }
  
  .stats-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@media (max-width: 480px) {
  .stats-grid {
    grid-template-columns: 1fr;
  }
  
  .feature-icon {
    display: block;
    margin: 0 0 1rem 0;
  }
  
  .feature-content {
    display: block;
    width: 100%;
  }
}

/* ===== SCROLLBAR ===== */
::-webkit-scrollbar {
  width: 8px;
}

::-webkit-scrollbar-track {
  background: #1a1a1a;
}

::-webkit-scrollbar-thumb {
  background: var(--primary-gradient);
  border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--secondary-gradient);
}
</style>
</head>

<body>
  <div class="container">
    <!-- Header Section -->
    <header class="header">
      <h1 class="main-title">NearMates</h1>
      <p class="subtitle">
        Connecting hearts, building communities, and bringing people together through shared passions and interests.
      </p>
    </header>

    <!-- Mission Statement -->
    <section class="section">
      <div class="content-card">
        <p>
          Human connection has always been at the center of our lives. But when people move to a new place, they often feel disconnected from their passions, hobbies, and the communities they once enjoyed. NearMates has been designed with a clear vision: to bring together individuals who share the same interests and make it easy for them to connect, collaborate, and create meaningful relationships. Whether it's sports, music, art, travel, coding, or casual hobbies — our platform ensures that nobody feels left out when they move to a new city or neighborhood.
        </p>
      </div>
    </section>

    <!-- Core Vision -->
    <section class="section">
      <h2 class="section-title">
        <i class="fas fa-lightbulb section-icon"></i>
        Core Vision of the Platform
      </h2>
      <ul class="feature-list">
        <li class="feature-item">
          <div class="feature-icon">
            <i class="fas fa-users"></i>
          </div>
          <div class="feature-content">
            <div class="feature-title">Interest-Based Networking</div>
            <div class="feature-description">
              People don't just want random social connections; they want meaningful connections with those who share the same passion. Our platform builds communities around hobbies and activities.
            </div>
          </div>
        </li>
        <li class="feature-item">
          <div class="feature-icon">
            <i class="fas fa-heart"></i>
          </div>
          <div class="feature-content">
            <div class="feature-title">Reducing Loneliness</div>
            <div class="feature-description">
              Many individuals feel isolated when shifting to a new environment. By joining groups and meeting like-minded individuals, they instantly feel at home.
            </div>
          </div>
        </li>
        <li class="feature-item">
          <div class="feature-icon">
            <i class="fas fa-handshake"></i>
          </div>
          <div class="feature-content">
            <div class="feature-title">Encouraging Collaboration</div>
            <div class="feature-description">
              From forming a local football team to creating a coding club, the platform gives people the tools to turn ideas into real activities.
            </div>
          </div>
        </li>
        <li class="feature-item">
          <div class="feature-icon">
            <i class="fas fa-shield-alt"></i>
          </div>
          <div class="feature-content">
            <div class="feature-title">Safe & Inclusive Environment</div>
            <div class="feature-description">
              Designed to prioritize digital safety, user privacy, and respectful communication for all community members.
            </div>
          </div>
        </li>
      </ul>
    </section>

    <!-- Why It's Needed -->
    <section class="section">
      <h2 class="section-title">
        <i class="fas fa-question-circle section-icon"></i>
        Why This Platform is Essential
      </h2>
      <div class="content-card">
        <p>
          Imagine moving to a completely new city. You want to play badminton, but you don't know anyone around. Or perhaps you are passionate about photography but have no clue where to find a community of photographers nearby. In such cases, most people end up leaving their passions behind. NearMates solves that exact problem by showing you who around you shares the same interest. You can directly connect with them, chat, form groups, and start engaging in your hobbies again — without waiting for months to discover people organically.
        </p>
      </div>
    </section>

    <!-- Stats Section -->
    <section class="section">
      <div class="stats-grid">
        <div class="stat-card">
          <span class="stat-number">10K+</span>
          <span class="stat-label">Active Users</span>
        </div>
        <div class="stat-card">
          <span class="stat-number">500+</span>
          <span class="stat-label">Interest Groups</span>
        </div>
        <div class="stat-card">
          <span class="stat-number">50+</span>
          <span class="stat-label">Cities Connected</span>
        </div>
        <div class="stat-card">
          <span class="stat-number">95%</span>
          <span class="stat-label">User Satisfaction</span>
        </div>
      </div>
    </section>

    <!-- Key Benefits -->
    <section class="section">
      <h2 class="section-title">
        <i class="fas fa-star section-icon"></i>
        Key Benefits
      </h2>
      <div class="content-card">
        <p>Here are the major benefits that make NearMates stand out:</p>
      </div>
      <ul class="feature-list">
        <li class="feature-item">
          <div class="feature-icon">
            <i class="fas fa-search-location"></i>
          </div>
          <div class="feature-content">
            <div class="feature-title">Precise Interest Matching</div>
            <div class="feature-description">Find people around you with the exact same interests and passion levels.</div>
          </div>
        </li>
        <li class="feature-item">
          <div class="feature-icon">
            <i class="fas fa-comments"></i>
          </div>
          <div class="feature-content">
            <div class="feature-title">Easy Communication</div>
            <div class="feature-description">Start conversations and plan activities without hesitation through our intuitive chat system.</div>
          </div>
        </li>
        <li class="feature-item">
          <div class="feature-icon">
            <i class="fas fa-users-cog"></i>
          </div>
          <div class="feature-content">
            <div class="feature-title">Community Building</div>
            <div class="feature-description">Form small communities based on hobbies like cricket, chess, coding, or singing.</div>
          </div>
        </li>
        <li class="feature-item">
          <div class="feature-icon">
            <i class="fas fa-bell"></i>
          </div>
          <div class="feature-content">
            <div class="feature-title">Event Notifications</div>
            <div class="feature-description">Get notifications about local meetups and events that match your interests.</div>
          </div>
        </li>
        <li class="feature-item">
          <div class="feature-icon">
            <i class="fas fa-brain"></i>
          </div>
          <div class="feature-content">
            <div class="feature-title">Mental Well-being</div>
            <div class="feature-description">Reduce feelings of isolation and boost mental well-being through meaningful connections.</div>
          </div>
        </li>
        <li class="feature-item">
          <div class="feature-icon">
            <i class="fas fa-globe"></i>
          </div>
          <div class="feature-content">
            <div class="feature-title">Cultural Exchange</div>
            <div class="feature-description">Encourage cultural exchange when people from different backgrounds come together.</div>
          </div>
        </li>
      </ul>
    </section>

    <!-- Use Cases -->
    <section class="section">
      <h2 class="section-title">
        <i class="fas fa-clipboard-list section-icon"></i>
        Real-World Success Stories
      </h2>
      <div class="content-card">
        <p>To better understand the platform's impact, here are some real examples of how NearMates transforms lives:</p>
      </div>
      <div class="use-case-grid">
        <div class="use-case-card">
          <div class="use-case-icon">
            <i class="fas fa-futbol"></i>
          </div>
          <h3 class="use-case-title">The Sport Enthusiast</h3>
          <p class="use-case-description">
            A student moves to a new city for college. They love football but have no team. By using NearMates, they instantly find other football lovers nearby and start playing again, forming lasting friendships.
          </p>
        </div>
        <div class="use-case-card">
          <div class="use-case-icon">
            <i class="fas fa-music"></i>
          </div>
          <h3 class="use-case-title">The Music Lover</h3>
          <p class="use-case-description">
            Someone with a passion for guitar playing finds others interested in jamming sessions and starts forming a music circle, leading to regular performances and collaborations.
          </p>
        </div>
        <div class="use-case-card">
          <div class="use-case-icon">
            <i class="fas fa-code"></i>
          </div>
          <div class="feature-content">
            <h3 class="use-case-title">The Tech Learner</h3>
            <p class="use-case-description">
              A beginner in coding discovers peers interested in hackathons and builds projects together, accelerating their learning and career growth.
            </p>
          </div>
        </div>
        <div class="use-case-card">
          <div class="use-case-icon">
            <i class="fas fa-running"></i>
          </div>
          <h3 class="use-case-title">Fitness Enthusiasts</h3>
          <p class="use-case-description">
            People looking for morning jogging partners or gym buddies quickly connect, maintaining their fitness goals while building a supportive community.
          </p>
        </div>
      </div>
    </section>

    <!-- Impact Statement -->
    <section class="section">
      <h2 class="section-title">
        <i class="fas fa-impact section-icon"></i>
        Why It Matters
      </h2>
      <div class="content-card">
        <p>
          In today's fast-paced world, digital platforms are usually blamed for reducing real-world interaction. But NearMates takes a different route. It uses technology not to separate people, but to bring them together in real life. It bridges the gap between online discovery and offline connection. By focusing on shared interests, it ensures conversations and meetups are more natural, engaging, and meaningful. Over time, this creates stronger communities and makes cities feel smaller, friendlier, and more welcoming.
        </p>
      </div>
    </section>

    <!-- Conclusion -->
    <section class="section">
      <h2 class="section-title">
        <i class="fas fa-rocket section-icon"></i>
        Join the Movement
      </h2>
      <div class="content-card">
        <p>
          NearMates is more than just a communication tool — it's a movement towards a connected society where no passion is left unexplored and no person feels disconnected. From sports and hobbies to professional skills and casual meetups, our platform is built to give everyone a chance to belong. It proves that no matter where you are, you can always find people who share your interests and passions.
        </p>
        <div style="text-align: center; margin-top: 2rem;">
          <a href="/dashboard" class="cta-button">
            <i class="fas fa-home"></i>
            Back to Dashboard
          </a>
        </div>
      </div>
    </section>
  </div>

  <script>
    // Smooth scrolling for navigation
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });

    // Intersection Observer for animations
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -100px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.animationPlayState = 'running';
          entry.target.classList.add('animate');
        }
      });
    }, observerOptions);

    // Observe all sections for animation
    document.querySelectorAll('.section').forEach((section, index) => {
      section.style.animationDelay = `${index * 0.1}s`;
      observer.observe(section);
    });

    // Add counter animation for stats
    function animateCounter(element, target) {
      let current = 0;
      const increment = target / 100;
      const timer = setInterval(() => {
        current += increment;
        if (current >= target) {
          current = target;
          clearInterval(timer);
        }
        
        if (target >= 1000) {
          element.textContent = Math.floor(current / 1000) + 'K+';
        } else if (target >= 100) {
          element.textContent = Math.floor(current) + '%';
        } else {
          element.textContent = Math.floor(current) + '+';
        }
      }, 20);
    }

    // Animate stats when they come into view
    const statsObserver = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const statNumber = entry.target.querySelector('.stat-number');
          const text = statNumber.textContent;
          let target = parseInt(text.replace(/[^\d]/g, ''));
          
          if (text.includes('%')) {
            target = parseInt(text);
          }
          
          animateCounter(statNumber, target);
          statsObserver.unobserve(entry.target);
        }
      });
    }, { threshold: 0.5 });

    document.querySelectorAll('.stat-card').forEach(card => {
      statsObserver.observe(card);
    });

    // Add parallax effect to cards
    window.addEventListener('scroll', () => {
      const scrolled = window.pageYOffset;
      const cards = document.querySelectorAll('.content-card, .use-case-card');
      
      cards.forEach((card, index) => {
        const speed = 0.02 + (index * 0.01);
        const yPos = -(scrolled * speed);
        card.style.transform = `translateY(${yPos}px)`;
      });
    });

    // Add hover sound effect simulation
    document.querySelectorAll('.feature-item, .use-case-card, .stat-card').forEach(item => {
      item.addEventListener('mouseenter', function() {
        this.style.transition = 'all 0.2s cubic-bezier(0.4, 0, 0.2, 1)';
      });
    });

    // Keyboard navigation support
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Tab') {
        document.body.classList.add('keyboard-navigation');
      }
    });

    document.addEventListener('mousedown', () => {
      document.body.classList.remove('keyboard-navigation');
    });
  </script>
</body>
</html>
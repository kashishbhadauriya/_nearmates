<!DOCTYPE html>
<html>
<head>
  <title>User Locations</title>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Leaflet CSS -->
  <link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css" />
  <link rel="stylesheet" href="https://unpkg.com/leaflet.markercluster/dist/MarkerCluster.css" />
  <link rel="stylesheet" href="https://unpkg.com/leaflet.markercluster/dist/MarkerCluster.Default.css" />

  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: #f4f7fb;
      text-align: center;
      color: #333;
    }
    h2 {
      margin: 20px 0;
      font-weight: 600;
      color: #444;
    }
    #map { 
      height: 85vh; 
      width: 90%; 
      margin: auto;
      border-radius: 15px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.15);
    }
    .leaflet-popup-content {
      font-size: 14px;
      font-weight: 500;
      color: #333;
    }
    .leaflet-popup-content b {
      color: #0077ff;
    }
    .chat-btn {
      display: inline-block;
      margin-top: 6px;
      padding: 6px 12px;
      background: #0077ff;
      color: white;
      border-radius: 8px;
      font-size: 13px;
      text-decoration: none;
      transition: background 0.2s ease-in-out;
    }
    .chat-btn:hover {
      background: #0056cc;
    }
  </style>
</head>
<body>
  <h2>📍 User Locations (within 100 km)</h2>
  <div id="map"></div>

  <script src="https://unpkg.com/leaflet/dist/leaflet.js"></script>
  <script src="https://unpkg.com/leaflet.markercluster/dist/leaflet.markercluster.js"></script>

  <script>
    var map = L.map('map').setView([20.5937, 78.9629], 5);
    L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', {
      attribution: '© OpenStreetMap contributors',
      maxZoom: 19
    }).addTo(map);

    var markers = L.markerClusterGroup();
    var defaultIcon = '/uploads/icon.png'; // Default icon if DP not uploaded

    function getDistanceFromLatLon(lat1, lon1, lat2, lon2) {
      const R = 6371; // Earth radius in km
      const dLat = (lat2 - lat1) * Math.PI / 180;
      const dLon = (lon2 - lon1) * Math.PI / 180;
      const a =
        Math.sin(dLat / 2) * Math.sin(dLat / 2) +
        Math.cos(lat1 * Math.PI / 180) *
        Math.cos(lat2 * Math.PI / 180) *
        Math.sin(dLon / 2) * Math.sin(dLon / 2);
      const c = 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
      return R * c; 
    }

    if (navigator.geolocation) {
      navigator.geolocation.getCurrentPosition(position => {
        let myLat = position.coords.latitude;
        let myLng = position.coords.longitude;

        // Current user marker
        const currentUserIcon = L.divIcon({
          className: 'current-user-icon',
          html: `<img src="/uploads/locationicon.png" alt="You" style="width:45px;height:45px;border-radius:50%;border:3px solid red;object-fit:cover;">`,
          iconSize: [45,45],
          iconAnchor: [22,45]
        });
        L.marker([myLat, myLng], {icon: currentUserIcon}).addTo(map)
          .bindPopup("<b>You are here</b>")
          .openPopup();

        map.setView([myLat, myLng], 13);

        // Save current user location to DB
        fetch('/save-location', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ latitude: myLat, longitude: myLng })
        }).catch(err => console.error(err));

        // Fetch other users
        fetch("/api/locations")
          .then(res => res.json())
          .then(users => {
            users.forEach(user => {
              if (!user.location || !user.location.latitude || !user.location.longitude) return;

              let dist = getDistanceFromLatLon(
                myLat, myLng,
                user.location.latitude, user.location.longitude
              );

              if (dist <= 100) {
                const dp = user.dp || defaultIcon;

                // User DP as marker icon
                const userIcon = L.divIcon({
                  className: 'user-icon',
                  html: `<img src="${dp}" alt="${user.name}" style="width:40px;height:40px;border-radius:50%;border:2px solid #0077ff;object-fit:cover;">`,
                  iconSize: [40,40],
                  iconAnchor: [20,40],
                  popupAnchor: [0,-40]
                });

                const popupHTML = `
                  <b>${user.name}</b><br>
                  🏀 Sport: ${user.interests || "Not specified"} <br>
                  📍 Distance: ${dist.toFixed(2)} km<br>
                  <a href="/chat/${user._id}" class="chat-btn">💬 Chat</a>
                `;

                L.marker([user.location.latitude, user.location.longitude], { icon: userIcon })
                  .bindPopup(popupHTML)
                  .addTo(markers);
              }
            });

            map.addLayer(markers);
          })
          .catch(err => console.error("Failed to fetch locations:", err));
      }, () => alert("Geolocation not supported or permission denied!"));
    } else {
      alert("Geolocation not supported by this browser.");
    }
  </script>
</body>
</html>

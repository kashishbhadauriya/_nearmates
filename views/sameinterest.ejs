<!DOCTYPE html>
<html>
<head>
  <title>Users with Same Interest</title>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Poppins:wght@400;500;600;700;800&display=swap" rel="stylesheet">

  <style>
    :root {
      --primary: #4F46E5;
      --primary-dark: #4338CA;
      --primary-light: #6366F1;
      --secondary: #10B981;
      --accent: #8B5CF6;
      --bg-primary: #FFFFFF;
      --bg-secondary: #F9FAFB;
      --bg-tertiary: #F3F4F6;
      --card-bg: #FFFFFF;
      --text-primary: #111827;
      --text-secondary: #6B7280;
      --text-tertiary: #9CA3AF;
      --border: #E5E7EB;
      --border-light: #F3F4F6;
      --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
      --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
      --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
      --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
      --radius-md: 12px;
      --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }

    body {
      background: var(--bg-secondary);
      min-height: 100vh;
      padding: 40px 20px;
      color: var(--text-primary);
    }

    .container { max-width: 1280px; margin: 0 auto; }

    /* Back Button */
    .back-button-container {
      position: fixed;
      top: 20px;
      left: 20px;
      z-index: 100;
    }
    .btn-chat {
      padding: 10px 20px;
      background: var(--bg-primary);
      color: var(--text-secondary);
      border: 1px solid var(--border);
      border-radius: var(--radius-md);
      cursor: pointer;
      font-weight: 500;
      font-size: 0.95rem;
      transition: var(--transition);
    }
    .btn-chat:hover {
      background: var(--bg-tertiary);
      color: var(--text-primary);
    }

    /* Header */
    .page-header {
      text-align: center;
      margin-bottom: 48px;
      margin-top: 40px;
    }
    h1 {
      font-family: 'Poppins', sans-serif;
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 12px;
    }
    .header-subtitle {
      font-size: 1.1rem;
      color: var(--text-secondary);
    }

    /* Error / Empty */
    .error, .no-users {
      font-size: 1rem;
      background: var(--card-bg);
      padding: 24px;
      border-radius: var(--radius-md);
      text-align: center;
      max-width: 600px;
      margin: 0 auto 20px;
      border: 2px dashed var(--border);
    }
    .error { color: #EF4444; background: #FEF2F2; }
    .no-users { color: var(--text-secondary); }

    /* Users Grid */
    .users-list {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 24px;
      width: 100%;
    }

    /* User Card */
    .user-card {
      background: var(--card-bg);
      border: 1px solid var(--border);
      border-radius: var(--radius-md);
      padding: 28px;
      text-align: center;
      transition: var(--transition);
      box-shadow: var(--shadow-sm);
      animation: fadeInUp 0.5s ease-out;
    }
    .user-card:hover {
      transform: translateY(-8px);
      box-shadow: var(--shadow-lg);
      border-color: var(--primary-light);
    }
    .user-card img {
      width: 96px;
      height: 96px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 20px;
      border: 3px solid var(--border-light);
      transition: var(--transition);
    }
    .user-card:hover img { transform: scale(1.05); border-color: var(--primary-light); }

    .user-card h2 { font-size: 1.25rem; margin-bottom: 12px; font-weight: 600; }
    .user-card p { font-size: 0.9rem; color: var(--text-secondary); margin-bottom: 10px; }

    .chat-btn {
      display: inline-block;
      margin-top: 16px;
      padding: 12px 32px;
      background: var(--primary);
      color: white;
      text-decoration: none;
      border-radius: var(--radius-md);
      font-weight: 600;
      transition: var(--transition);
    }
    .chat-btn:hover { background: var(--primary-dark); transform: translateY(-2px); }

    .interest-badge {
      display: inline-block;
      padding: 6px 16px;
      background: #EEF2FF;
      color: var(--primary);
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: 600;
      margin-bottom: 16px;
    }

    @keyframes fadeInUp {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="back-button-container">
      <button type="button" class="btn-chat" onclick="window.history.back()">← back</button>
    </div>

    <div class="page-header">
      <h1>Users with the same interest:</h1>
    </div>

    <% if (typeof error !== 'undefined' && error) { %>
      <div class="error">
        <div class="error-icon">⚠️</div>
        <%= error %>
      </div>
    <% } else if (users && users.length === 0) { %>
      <div class="no-users">
        <div class="no-users-icon">👥</div>
         <script>
    window.location.href = "/alluser";
  </script>
      </div>
    <% } else if (users && users.length > 0) { %>
      <div class="users-list">
        <% users.forEach(function(user) { %>
          <div class="user-card">
            <img src="<%= user.dp || '/uploads/icon.png' %>" alt="<%= user.name %>">
            <h2><%= user.name %></h2>
            <span class="interest-badge"><%= user.interests %></span>
            <p><strong>City:</strong> <%= user.city || 'Not specified' %></p>
            <p><strong>About:</strong> <%= user.about || 'No info' %></p>

            <a href="/chat/<%= user._id %>" class="chat-btn">Message</a>

           <% if (user.status === 'friends') { %>
  <button disabled class="add-friend-btn" style="
    background: linear-gradient(135deg, #10B981, #34D399);
    color: white;
    border: none;
    border-radius: 8px;
    padding: 8px 16px;
    cursor: not-allowed;
    font-weight: 600;
    box-shadow: 0 2px 6px rgba(16,185,129,0.4);
  ">Friends</button>

<% } else if (user.status === 'sent') { %>
  <button disabled class="add-friend-btn" style="
    background: linear-gradient(135deg, #F59E0B, #FBBF24);
    color: white;
    border: none;
    border-radius: 8px;
    padding: 8px 16px;
    cursor: not-allowed;
    font-weight: 600;
    box-shadow: 0 2px 6px rgba(245,158,11,0.4);
  ">Request Sent</button>

<% } else if (user.status === 'pending') { %>
  <button disabled class="add-friend-btn" style="
    background: linear-gradient(135deg, #3B82F6, #60A5FA);
    color: white;
    border: none;
    border-radius: 8px;
    padding: 8px 16px;
    cursor: not-allowed;
    font-weight: 600;
    box-shadow: 0 2px 6px rgba(59,130,246,0.4);
  "> Pending</button>

<% } else { %>
  <form action="/friend-request/<%= user._id %>" method="POST" style="display:inline;">
    <button type="submit" class="add-friend-btn" style="
      background: linear-gradient(135deg, #6366F1, #8B5CF6);
      color: white;
      border: none;
      border-radius: 8px;
      padding: 8px 16px;
      font-weight: 600;
      cursor: pointer;
      transition: all 0.3s ease;
      box-shadow: 0 2px 6px rgba(99,102,241,0.4);
    " 
    onmouseover="this.style.transform='scale(1.05)'"
    onmouseout="this.style.transform='scale(1)'">
       Add Friend
    </button>
  </form>
<% } %>
          </div>
        <% }) %>
      </div>
    <% } %>
  </div>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const cards = document.querySelectorAll('.user-card');
      cards.forEach((card, i) => {
        card.style.opacity = '0';
        card.style.transform = 'translateY(30px)';
        setTimeout(() => {
          card.style.transition = 'all 0.6s ease';
          card.style.opacity = '1';
          card.style.transform = 'translateY(0)';
        }, i * 100);
      });
    });
  </script>
</body>
</html>
